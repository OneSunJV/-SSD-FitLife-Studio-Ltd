import sqlite3

# connection = sqlite3.connect('JV_Database.db') # Connection to JV_Database.db created.
# cursor_object = connection.cursor() # Allows us to execute SQL queries.
# cursor_object.execute('''SELECT * FROM table_names WHERE field1 LIKE ? AND field2 LIKE ? AND field3 LIKE ? AND field4 LIKE ? AND field5 LIKE ? AND field6 LIKE ?''', 
#                                 (field1_parameter, field2_parameter, field3_parameter, field4_parameter, field5_parameter, field6_parameter,)) # Searches for all orders that meet the filters entered by the user.
# data = cursor_object.fetchall() # Stores the 
# connection.close() # Connection to JV_Database.db terminated.